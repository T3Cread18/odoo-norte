<!-- <?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_commissions_tree" model="ir.ui.view">
        <field name="name">commissions.tree</field>
        <field name="model">accounts.payable.crm</field>
        <field name="arch" type="xml">
            <tree string="Comisiones" create="false" edit="false">
                <field name="user_id"/>
                <field name="total_amounts" sum="Total"/>
                <!-- Otros campos -->
            </tree>
           <tree string="Comisiones" create="false" edit="false">
                <field name="user_id"/>
                <field name="total_amounts" sum="Total"/>
                <!-- Otros campos -->
            </tree>
        </field>
    </record>

    <record id="action_commissions" model="ir.actions.act_window">
        <field name="name">action_commissions</field>
        <field name="res_model">accounts.payable.crm</field>
        <field name="view_mode">tree</field>
        <field name="view_id" ref="view_commissions_tree"/>
        <field name="domain">[('state', '=', 'payment')]</field>
    </record>

    <record id="menu_comisiones" model="ir.ui.menu">
        <field name="name">Comisiones</field>
        <field name="action" ref="action_commissions"/>
        <field name="sequence" eval="10"/>
    </record>
</odoo> -->